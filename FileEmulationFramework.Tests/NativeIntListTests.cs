namespace FileEmulationFramework.Tests;

public class NativeIntListTests
{
    /*
    [Fact]
    public unsafe void Baseline()
    {
        var list = new NativeIntList();
        for (int x = 0; x < 100; x++)
            list.AddItem(x);
        
        for (int x = 0; x < 100; x++)
            Assert.Equal(x, (int)list.ListPtr[x]);
        
        list.Free((nuint)list.ListPtr);
    }
    */
}